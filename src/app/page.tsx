'use client'

export default function HomePage() {
  // The Navigation component handles the complete dashboard layout when on home page
  // This component returns an empty fragment since Navigation renders the full dashboard
  return <></>
}
