# =========================================
# POS DOMINICANA - PRODUCTION ENVIRONMENT
# =========================================

# Database Configuration
# Replace with your production PostgreSQL connection string
DATABASE_URL="postgresql://username:password@host:5432/pos_dominicana_prod?schema=public"

# Security Keys (CRITICAL: Generate secure 32+ character strings)
# Use: openssl rand -base64 32
JWT_SECRET="REPLACE_WITH_SECURE_32_PLUS_CHARACTER_STRING"
NEXTAUTH_SECRET="REPLACE_WITH_SECURE_32_PLUS_CHARACTER_STRING"

# Application Configuration
NODE_ENV="production"
NEXTAUTH_URL="https://yourdomain.com"

# Business Configuration
BUSINESS_NAME="Tu Empresa SRL"
BUSINESS_RNC="130123456789"
BUSINESS_ADDRESS="Tu Dirección Comercial, Santo Domingo, RD"
BUSINESS_PHONE="+1-809-555-0123"
BUSINESS_EMAIL="info@tuempresa.com"

# DGII Integration
DGII_API_URL="https://api.dgii.gov.do/rnc"
DGII_SYNC_ENABLED="true"
DGII_SYNC_INTERVAL="86400000"  # 24 hours in milliseconds

# Email Configuration (for receipts and notifications)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
EMAIL_FROM="POS Sistema <noreply@tuempresa.com>"

# File Upload Configuration
MAX_FILE_SIZE="10485760"  # 10MB in bytes
UPLOAD_PATH="/uploads"

# Rate Limiting
RATE_LIMIT_MAX="100"      # requests per window
RATE_LIMIT_WINDOW="60000" # 1 minute in milliseconds

# Logging Configuration
LOG_LEVEL="info"          # error, warn, info, debug
LOG_FILE_PATH="/var/log/pos-dominicana"
LOG_MAX_SIZE="20m"        # Maximum log file size
LOG_MAX_FILES="10"        # Number of log files to keep

# Security Configuration
CORS_ORIGIN="https://yourdomain.com"
SECURE_COOKIES="true"
SESSION_TIMEOUT="3600000" # 1 hour in milliseconds

# Performance Configuration
DATABASE_POOL_MIN="2"
DATABASE_POOL_MAX="10"
CACHE_TTL="300000"        # 5 minutes in milliseconds

# Backup Configuration
BACKUP_ENABLED="true"
BACKUP_INTERVAL="daily"   # daily, weekly, monthly
BACKUP_RETENTION="30"     # days to keep backups
BACKUP_PATH="/backups"

# Monitoring Configuration
HEALTH_CHECK_ENABLED="true"
METRICS_ENABLED="true"
ERROR_TRACKING_DSN=""     # Sentry DSN if using error tracking

# Development/Staging Overrides (remove in production)
# Uncomment for staging environment
# NODE_ENV="staging"
# NEXTAUTH_URL="https://staging.yourdomain.com"

# =========================================
# DEPLOYMENT NOTES:
# =========================================
# 1. Replace ALL placeholder values above
# 2. Generate secure random strings for JWT_SECRET and NEXTAUTH_SECRET
# 3. Configure your production database URL
# 4. Set up SSL certificate for HTTPS
# 5. Configure your domain in NEXTAUTH_URL and CORS_ORIGIN
# 6. Test all configurations before going live
# 7. Never commit this file to version control with real secrets
# =========================================
